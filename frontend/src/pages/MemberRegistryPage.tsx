import React from 'react';
import { Typography } from '@mui/material';

const MemberRegistryPage: React.FC = () => {
  return <Typography variant="h4">Member Registry</Typography>;
};

export default MemberRegistryPage;
